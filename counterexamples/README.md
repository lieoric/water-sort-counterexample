# Verified counterexamples

No 5x16 counterexample has been verified yet.

When one is found, commit both the bottom-to-top instance text file and its
`.wscert` transition-closure certificate here. The `Verify counterexample`
workflow will rebuild the verifier and check them.
